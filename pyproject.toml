[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "agentihub"
version = "0.1.0"
description = "Private agent identities for the agenti* ecosystem"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["agents*"]
