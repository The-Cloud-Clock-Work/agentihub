name: publishing
description: "Content publishing agent — drafts, reviews, publishes to LinkedIn/Medium"

claude:
  model: sonnet
  max_turns: 40
  permission_mode: bypassPermissions
  no_session_persistence: true
  output_format: json
  worktree: false
  effort: high
  timeout: 1800

auto_pr: false
mcp_categories: all
